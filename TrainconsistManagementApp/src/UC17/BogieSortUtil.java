package UC17;

import java.util.Arrays;

public class BogieSortUtil {

    // Sort bogie names using Arrays.sort()
    public static void sortBogieNames(String[] bogieNames) {
        Arrays.sort(bogieNames);
    }
}